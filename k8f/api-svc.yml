apiVersion: v1
kind: Service
metadata:
  name: api-service-svc
  namespace: myapp
spec:
  type: ClusterIP
  selector:
    app: api-service
  ports:
    - port: 80        # Service port (cluster internal)
      targetPort: 3000  # Container port